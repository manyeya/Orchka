// HTTP Request Node - React Flow component
// This is a placeholder that shows the component structure
// To be fully migrated, copy node.tsx from apps/web/features/nodes/action/https-request/

import type { Node, NodeProps } from "@xyflow/react";
import type { HttpSettingsFormValues } from "./types";

type HttpRequestNodeType = Node<HttpSettingsFormValues>;

// TODO: Copy full implementation from apps/web/features/nodes/action/https-request/node.tsx
export function HttpRequestNode(props: NodeProps<HttpRequestNodeType>) {
    return null;
}
