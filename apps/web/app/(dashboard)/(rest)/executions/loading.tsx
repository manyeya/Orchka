import { EntityLoader } from "@/components/entity-component";

export default function Loading() {
  return <EntityLoader title="Loading Executions" description="Fetching your execution history..." />;
}
